'use strict';

const adalaber1 = {
    name: 'Susana',
    age: 34,
    career: 'periodista'
};

adalaber1.run = distance => `Estoy corriendo una maratón de ${distance} kilómentros`;
console.log (adalaber1.run(50));

// const sentenceElement = document.querySelector('.sentence');

// sentenceElement.innerHTML = (`Mi nombre es ${adalaber1.name}, tengo ${adalaber1.age} y soy ${adalaber1.career}`);

// adalaber1 = {};
// adalaber1.